// import { createSlice } from "@reduxjs/toolkit";
// import { RootState } from "../index.ts";

// const refreshIndicatorSlice = createSlice({
//   name: "refreshIndicator",
//   initialState: 0,
//   reducers: {
//     refresh: (state) => {
//       return ++state;
//     },
//   },
// });

// export const { refresh } = refreshIndicatorSlice.actions;

// export const refreshSelect = (state: RootState) => state.refreshIndicator;

// export default refreshIndicatorSlice.reducer;
