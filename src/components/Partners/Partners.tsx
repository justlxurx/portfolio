import s from "./Partners.module.scss";

export const Partners = () => {
  return (
    <section className={`${s.main} container`}>
      <h2 className={s.main__title}>Our partners</h2>
      <div className={s.main__content}></div>
    </section>
  );
};
