worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        location /python {
            root /usr/share/nginx/html;
            index python.html;
        }

        location /react {
            root /usr/share/nginx/html;
            index react.html;
        }
    }
}